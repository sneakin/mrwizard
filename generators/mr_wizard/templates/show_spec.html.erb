require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "<%= name %>/show.html.erb" do
  it "has a form that posts to the wizard\'s URL"
  it "has a hidden field with the wizard\'s step"

  describe 'when the wizard is NOT done' do
    it "renders the partial for the wizard\'s step passing it the wizard and form builder"
    it "has a submit button labeled next"
  end

  describe 'when the wizard is done' do
    it "renders the done partial"
    it "does not have a submit button"
  end
end
